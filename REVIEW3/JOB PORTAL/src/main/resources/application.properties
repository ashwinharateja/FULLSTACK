# Spring Boot — application.properties
spring.application.name=nexus-jobs

# ── H2 In-Memory Database ──
spring.datasource.url=jdbc:h2:mem:nexusjobs;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# ── JPA / Hibernate ──
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# ── H2 Console (access at /h2-console) ──
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.h2.console.settings.web-allow-others=false

# ── Thymeleaf ──
spring.thymeleaf.cache=false
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.mode=HTML

# ── Server ──
server.port=8080
server.servlet.session.timeout=30m

# ── Logging ──
logging.level.com.nexusjobs=INFO
logging.level.org.springframework.security=WARN

# ── Spring Mail (Gmail SMTP) ──
# SETUP STEPS:
#  1. Enable 2-Step Verification on your Google account.
#  2. Go to: https://myaccount.google.com/apppasswords
#  3. Create an app password (App = Mail, Device = Other → name it "NexusJobs").
#  4. Copy the 16-character password (no spaces) and paste it below.
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=ashwinharateja@gmail.com
spring.mail.password=gitt kdjg ztwx hokx
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ── OTP Settings ──
# Expiry in minutes
otp.expiry-minutes=5
# dev-mode=false → OTP is ONLY sent by email (real-time). Set true to also print to console.
otp.dev-mode=false
